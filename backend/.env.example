# Database
DATABASE_URL=postgresql://user:password@localhost:5432/wallet_dashboard

# App
NODE_ENV=development
PORT=3001

# Les variables Auth / Encryption / Revolut / Etherscan / Solana / Pokemon TCG
# seront ajoutées ici à l'étape "Backend core" (module auth + intégrations).
